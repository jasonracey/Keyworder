@page "/"
@inject NavigationManager _navigator

<div class="spinner-border" role="status">
    <span class="sr-only">Loading...</span>
</div>

@code {
    protected override void OnInitialized()
    {
        _navigator.NavigateTo("/select-keywords", replace: true);
    }
}